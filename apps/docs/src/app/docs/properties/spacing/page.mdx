import { Box, Flex, Text } from "@locus-ui/components";
import { ComponentPreview } from "@/components/component-preview";
import { PreviewBox } from "@/components/preview-box";
import {
  indexSnippet,
  spacingInheritanceSnippet,
  spacingPropertySnippet,
} from "./snippets";

export const spacingValues = ["xs", "sm", "md", "lg", "xl"];

# Spacing Property

Controls the overall spacing scale for elements. Supports scaled values and inheritance from parent elements.

<ComponentPreview
  codeSnippets={[
    {
      title: "index.tsx",
      code: indexSnippet,
    },
  ]}
  source="https://github.com/LocusInc/locus-ui/tree/main/packages/locus-ui/src/props"
>
  <Box spacing="lg" p="4" className="border border-[rgb(var(--border-1))]">
    <Text>Large Spacing</Text>
  </Box>
</ComponentPreview>

## Examples

### Spacing Scale

The `spacing` prop sets the overall spacing scale for an element: `xs`, `sm`, `md`, `lg`, or `xl`.

<PreviewBox title="spacing-property.tsx" code={spacingPropertySnippet}>
  <Flex gap="2" align="center">
    {spacingValues.map((size) => (
      <Box
        p="4"
        key={size}
        className="border border-[rgb(var(--border-1))]"
        spacing={size}
      >
        {size.toUpperCase()}
      </Box>
    ))}
  </Flex>
</PreviewBox>

### Spacing Inheritance

Child elements use the theme spacing unless they are given `spacing="inherit"`.

<PreviewBox title="spacing-inheritance.tsx" code={spacingInheritanceSnippet}>
  <Box
    p="2"
    m="2"
    spacing="xl"
    className="border border-[rgb(var(--border-1))]"
  >
    Parent Box (XL Spacing, p=2, m=2)
    <Box 
      p="2" 
      m="2"
      spacing="inherit"
      className="border border-[rgb(var(--border-1))]" 
    >
      Child 1 (Inherited Spacing, p=2, m=2)
    </Box>

    <Box
      p="2"
      m="2"
      className="border border-[rgb(var(--border-1))]"
    >
      Child 2 (Default Spacing, p=2, m=2)
    </Box>

  </Box>
</PreviewBox>
