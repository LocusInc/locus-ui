import { CodeBox } from "@/components/code-box";

export const installCode = `npm install @locus-ui/components`;

export const quickStartCode = `import { Theme } from "@locus-ui/components";
import "@locus-ui/components/styles";

export default function App({ children }) {
  return (
    <Theme appearance="light" radius="md" spacing="md">
      {children}
    </Theme>
  );
}`;

# Getting Started

## Installation

Install the package from npm. React 18 or 19 is required as a peer dependency.

<CodeBox title="Terminal" language="bash" className="my-2">
  {installCode}
</CodeBox>

## Quick Start

Import the stylesheet and wrap your app with the `Theme` provider to get started:

<CodeBox title="app.tsx" language="typescript" className="my-2">
  {quickStartCode}
</CodeBox>
