import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Locus UI | Documentation",
  description: "An (optionally) unstyled React component library",
};

export { default } from "../../root-pages/documentation.page";
